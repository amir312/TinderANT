import React, { Component } from 'react'

function user(props) {
  name:{props.name}
  age:
        return (
            <div>
                
            </div>
        )
    }

export default user
